# Web framework
fastapi>=0.110.0
uvicorn>=0.27.0
# Pinned below 2.0: the schemas are written against the pydantic v1 API
pydantic>=1.10.0,<2.0

# HTTP (requests was previously only a transitive dependency — it is the
# primary HTTP client and must be declared directly; urllib3 is imported
# via requests' adapters)
requests>=2.31.0
urllib3>=1.26.0

# Auth / DRM
PyJWT>=2.4.0
pywidevine>=1.4.0

# Configuration
python-dotenv>=0.19.0

# Dev / test
pytest>=8.0.0
ruff
